# 駅掲示板 アプリ仕様書

## 1. アプリ概要

| 項目 | 内容 |
|------|------|
| アプリ名 | ここ板（ここばん） |
| フレームワーク | Flutter (Dart SDK ^3.11.0) |
| 状態管理 | flutter_riverpod ^2.6.1 |
| バックエンド | Supabase (supabase_flutter ^2.8.0) |
| バージョン | 1.0.0+1 |
| デザイン | Material Design 3 (Material You) |
| 対応プラットフォーム | iOS / Android / Web / macOS / Linux / Windows |

## 2. コンセプト

ユーザーの現在地から半径500m以内にある鉄道駅を検索し、各駅に紐づいた匿名掲示板でスレッド作成・返信ができるアプリ。ユーザー登録・ログインは不要で、完全匿名で利用可能。

---

## 3. 使用ライブラリ

| パッケージ | バージョン | 用途 |
|-----------|-----------|------|
| `flutter_riverpod` | ^2.6.1 | 状態管理 |
| `supabase_flutter` | ^2.8.0 | バックエンド (DB/API) |
| `geolocator` | ^13.0.2 | 位置情報の取得 |
| `intl` | ^0.20.2 | 日時フォーマット |
| `cupertino_icons` | ^1.0.8 | iOS風アイコン |

---

## 4. アーキテクチャ

### 4.1 ディレクトリ構成

```
lib/
├── main.dart                  # エントリーポイント
├── models/                    # データモデル
│   ├── station.dart           # 駅モデル
│   ├── thread.dart            # スレッドモデル
│   ├── post.dart              # 投稿（返信）モデル
│   └── report.dart            # 通報モデル
├── services/                  # データ取得・操作サービス
│   ├── supabase_service.dart  # Supabase初期化・接続管理
│   ├── station_service.dart   # 駅データ取得
│   ├── thread_service.dart    # スレッドCRUD
│   ├── post_service.dart      # 投稿CRUD
│   ├── report_service.dart    # 通報送信
│   └── location_service.dart  # 位置情報サービス
├── providers/                 # Riverpod プロバイダー
│   ├── location_provider.dart # 位置情報状態管理
│   ├── station_provider.dart  # 近隣駅状態管理
│   └── thread_provider.dart   # スレッド・投稿状態管理
├── widgets/                   # 再利用可能ウィジェット
│   ├── station_card.dart      # 駅カード
│   ├── thread_card.dart       # スレッドカード
│   └── post_item.dart         # 投稿アイテム
└── screens/                   # 画面
    ├── home_screen.dart       # ホーム画面（近隣駅一覧）
    ├── board_screen.dart      # 掲示板画面（スレッド一覧）
    ├── create_thread_screen.dart # スレッド作成画面
    ├── thread_detail_screen.dart # スレッド詳細・返信画面
    └── settings_screen.dart   # 設定画面
```

### 4.2 設計パターン

- **Service層**: データの取得・送信を担当（Supabase通信 or モックデータ）
- **Provider層**: Riverpod AsyncNotifier による非同期状態管理
- **Widget/Screen層**: UI表示

---

## 5. データモデル

### 5.1 Station（駅）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `id` | `String` | 駅の一意識別子 |
| `name` | `String` | 駅名 |
| `latitude` | `double` | 緯度 |
| `longitude` | `double` | 経度 |
| `prefecture` | `String` | 都道府県 |
| `lineName` | `String` | 路線名 |
| `distance` | `double?` | 現在地からの距離（メートル）※任意 |

### 5.2 Thread（スレッド）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `id` | `String` | スレッドの一意識別子 |
| `stationId` | `String` | 紐づく駅のID |
| `title` | `String` | スレッドタイトル（最大50文字） |
| `body` | `String` | スレッド本文（最大1000文字） |
| `createdAt` | `DateTime` | 作成日時 |
| `postCount` | `int` | 返信数（デフォルト: 0） |

### 5.3 Post（投稿/返信）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `id` | `String` | 投稿の一意識別子 |
| `threadId` | `String` | 紐づくスレッドのID |
| `body` | `String` | 投稿本文（最大1000文字） |
| `createdAt` | `DateTime` | 作成日時 |

### 5.4 Report（通報）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `id` | `String` | 通報の一意識別子 |
| `contentType` | `String` | 対象種別（`'thread'` or `'post'`） |
| `contentId` | `String` | 対象コンテンツのID |
| `reason` | `String` | 通報理由 |
| `details` | `String?` | 詳細（任意） |

---

## 6. 画面仕様

### 6.1 ホーム画面 (`HomeScreen`)

- **AppBar**: タイトル「駅掲示板」、右に設定アイコン
- **起動時の動作**: 位置情報の権限を要求 → 現在地を取得 → 半径500m以内の近隣駅を検索
- **Pull-to-Refresh**: 下に引っ張って位置情報と駅一覧を再取得
- **駅一覧**: `StationCard`をリスト表示（駅名、路線名、距離）
- **タップ**: 該当駅の掲示板画面(`BoardScreen`)へ遷移

#### 状態別表示

| 状態 | 表示内容 |
|------|----------|
| 位置情報取得中 | プログレスインジケーター + 「位置情報を取得中...」 |
| 位置情報未許可（初回） | アイコン + 説明テキスト + 「位置情報を取得」ボタン |
| 位置情報拒否 | エラーアイコン + 「設定アプリから位置情報の使用を許可してください」 + 「再試行」ボタン |
| 駅が見つからない | 「500m以内に駅がありません」メッセージ |
| エラー | エラーアイコン + エラーメッセージ表示 |

### 6.2 掲示板画面 (`BoardScreen`)

- **AppBar**: タイトル「{駅名}駅」
- **スレッド一覧**: `ThreadCard`をリスト表示（タイトル、本文プレビュー、返信数、作成日時）
- **Pull-to-Refresh**: スレッド一覧を再取得
- **FAB**: 「スレッド作成」ボタン → `CreateThreadScreen`へ遷移
- **タップ**: 該当スレッドの詳細画面(`ThreadDetailScreen`)へ遷移
- **空状態**: 「まだスレッドがありません」メッセージ

### 6.3 スレッド作成画面 (`CreateThreadScreen`)

- **AppBar**: タイトル「スレッド作成」、右に「投稿」ボタン
- **入力フォーム**:
  - タイトル: 必須、最大50文字、文字数カウンター表示
  - 本文: 必須、最大1000文字、文字数カウンター表示、4〜8行入力欄
- **注意書き**: 「投稿は匿名で行われます。個人情報の記載はお控えください。」
- **バリデーション**: タイトル・本文が空の場合はエラーメッセージ表示
- **送信中**: ボタンがプログレスインジケーターに変化、二重送信防止

### 6.4 スレッド詳細画面 (`ThreadDetailScreen`)

- **AppBar**: スレッドタイトル（省略表示対応）、右に通報アイコン
- **スレッド本文エリア**: タイトル（太字）、本文、作成日時を表示
- **返信一覧**: `PostItem`をリスト表示
  - 連番表示（1, 2, 3...）
  - 投稿本文
  - 相対時間表示（○分前、○時間前、MM/dd HH:mm）
  - ポップアップメニューから「通報する」
- **返信入力フォーム**: 画面下部に固定
  - テキスト入力欄（最大1000文字、1〜3行）
  - 送信ボタン（IconButton.filled）
  - 送信後は自動で最下部にスクロール
- **空状態**: 「まだ返信がありません」メッセージ

### 6.5 設定画面 (`SettingsScreen`)

- **AppBar**: タイトル「設定」
- **情報セクション**:
  - 利用規約（未実装 → SnackBar「利用規約は準備中です」）
  - プライバシーポリシー（未実装 → SnackBar「プライバシーポリシーは準備中です」）
- **アプリセクション**:
  - バージョン: 1.0.0

---

## 7. 機能詳細

### 7.1 位置情報機能

- **使用ライブラリ**: `geolocator`
- **精度**: `LocationAccuracy.high`
- **権限チェック**: `whileInUse` または `always` を確認
- **権限要求フロー**: サービス有効確認 → 権限チェック → 未許可なら要求 → 永久拒否の場合は失敗
- **距離計算**: Haversine公式を用いた2点間距離計算（メートル単位）

### 7.2 近隣駅検索

- **検索範囲**: 半径500m（デフォルト）
- **Supabase接続時**: RPC `get_nearby_stations` を呼び出し
- **モックモード**: 東京駅(120m)、有楽町駅(350m)、神田駅(480m) の3駅を返却

### 7.3 スレッド管理

- **一覧取得**: 駅IDに紐づくスレッドを作成日時降順で取得
- **新規作成**: 駅ID、タイトル、本文を指定して作成
- **作成後**: スレッド一覧を自動リフレッシュ

### 7.4 投稿（返信）管理

- **一覧取得**: スレッドIDに紐づく投稿を作成日時昇順で取得
- **新規投稿**: スレッドID、本文を指定して作成
- **投稿後**: 投稿一覧を自動リフレッシュ + 最下部自動スクロール

### 7.5 通報機能

- **対象**: スレッドおよび個別投稿
- **通報理由（選択式）**:
  1. スパム・宣伝
  2. 不適切な内容
  3. 誹謗中傷
  4. 個人情報の掲載
  5. その他
- **通報後**: SnackBar「通報を受け付けました」を表示
- **操作方法**:
  - スレッド通報: AppBar右の旗アイコン
  - 投稿通報: 各投稿のポップアップメニュー → 「通報する」

---

## 8. テーマ・UI仕様

### 8.1 テーマ設定

| 項目 | 値 |
|------|-----|
| カラーシード | `#4299F0`（ブルー系） |
| デザインシステム | Material 3 |
| テーマモード | システム設定に追従（ライト/ダーク自動切替） |
| デバッグバナー | 非表示 |

### 8.2 日時表示フォーマット

| 条件 | 表示形式 | 使用箇所 |
|------|----------|----------|
| 60分未満 | `○分前` | ThreadCard, PostItem |
| 24時間未満 | `○時間前` | ThreadCard, PostItem |
| 7日未満 | `○日前` | ThreadCard |
| 7日以上 | `yyyy/MM/dd` | ThreadCard |
| 24時間以上 | `MM/dd HH:mm` | PostItem |
| スレッド詳細 | `yyyy/MM/dd HH:mm` | ThreadDetailScreen |

---

## 9. バックエンド (Supabase)

### 9.1 モックモード

現在 `_useMock = true` がデフォルトで、Supabaseに接続せずにモックデータで動作する。`SupabaseService.initialize()` が呼ばれるとモックモードが解除される（現在 `main.dart` では呼び出されていない）。

### 9.2 Supabaseテーブル（想定）

| テーブル名 | 用途 |
|-----------|------|
| `stations` | 駅マスターデータ（RPC経由でアクセス） |
| `threads` | スレッドデータ |
| `posts` | 投稿データ |
| `reports` | 通報データ |

### 9.3 Supabase RPC（想定）

| 関数名 | パラメータ | 用途 |
|--------|-----------|------|
| `get_nearby_stations` | `lat`, `lng`, `radius_meters` | 指定座標の近隣駅を取得 |

---

## 10. 未実装・TODO項目

| 項目 | 状態 | 備考 |
|------|------|------|
| Supabase接続 | 未実装 | `main.dart`で`SupabaseService.initialize()`が未呼び出し |
| 利用規約ページ | 未実装 | SnackBar「準備中」を表示 |
| プライバシーポリシーページ | 未実装 | SnackBar「準備中」を表示 |
| ユーザー認証 | なし | 完全匿名設計 |
| 画像投稿 | なし | テキストのみ |
| プッシュ通知 | なし | - |
| スレッド検索 | なし | - |
| 投稿の編集・削除 | なし | - |
| ブロック/ミュート | なし | - |

---

## 11. 入力制限

| 項目 | 最大文字数 |
|------|-----------|
| スレッドタイトル | 50文字 |
| スレッド本文 | 1,000文字 |
| 返信本文 | 1,000文字 |

---

## 12. 拡張予定仕様（事業戦略に基づく）

> 以下は `docs/business-strategy-v2.md` の実証計画に基づき、今後追記が必要な技術仕様の一覧。
> 各項目は実装着手時に詳細を本書に追記すること。

### 12.1 Location抽象化（イベント対応）

現在の `Station` モデルを `Location` に抽象化し、イベント会場を掲示板の対象にできるようにする。

```
Location
 ├─ station（常設 … 既存の駅モデル）
 ├─ event（期間限定 … フェス・花火等）
 └─ area（将来拡張）
```

**追記が必要な項目:**
- [ ] `locations` テーブル設計（type, radius, start_date, end_date, is_active）
- [ ] 既存の `Station` モデルとの互換性・マイグレーション方針
- [ ] Supabase RPC の変更（`get_nearby_stations` → Location対応）

### 12.2 Web版

QRコードからアクセスする軽量Webページ。ネイティブアプリとは別に構築する。

**追記が必要な項目:**
- [ ] 技術選定（Next.js / SvelteKit / 静的HTML+JS）
- [ ] 画面構成（掲示板ページ、スレッドページ、利用規約、プライバシーポリシー）
- [ ] URL設計（例: `/board/{location_id}`, `/board/{location_id}/thread/{thread_id}`）
- [ ] UTMパラメータの設計
- [ ] ホスティング先（Vercel / Cloudflare Pages 等）

### 12.3 計測基盤

**追記が必要な項目:**
- [ ] Firebase Analytics の導入手順（Web版）
- [ ] Supabase Realtime Presence による同時接続数の計測実装
- [ ] UTMパラメータの取得・記録方法
- [ ] KPI集計用SQLクエリ一覧
