# 開発ガードレール

> このファイルは Claude Code が毎セッション自動読み込みするルールファイルです。
> すべての開発作業はこのルールに従って行うこと。

---

## 1. 絶対禁止事項（NEVER）

### 1.1 スコープ逸脱の禁止
- **指示されていない機能を勝手に追加しない**
- **指示されていないファイルを勝手に作成しない**
- **指示されていないリファクタリングを行わない**
- **「ついでに」「ちなみに」で余計な変更を加えない**
- 変更対象外のファイルに手を出さない

### 1.2 破壊的変更の禁止
- 既存の動作を壊す変更を無断で行わない
- データモデルのフィールド削除・型変更は必ず確認を取る
- 既存のAPIインターフェース（Service層のメソッドシグネチャ）を無断で変更しない
- パッケージの削除・メジャーバージョン変更は必ず確認を取る

### 1.3 セキュリティ
- APIキー・シークレットをソースコードにハードコードしない
- `.env` ファイルや認証情報をコミット対象に含めない
- ユーザー入力を未サニタイズのまま使用しない

### 1.4 Git操作
- `git push` を無断で実行しない
- `main` / `master` ブランチに直接コミットしない（指示がある場合を除く）
- force push を行わない
- コミットは必ずユーザーの明示的な指示を受けてから行う

---

## 2. 必須確認事項（MUST ASK）

以下の作業を行う前に、**必ずユーザーに確認を取ること**:

- [ ] 新しい画面（Screen）の追加
- [ ] 新しいデータモデル（Model）の追加・変更
- [ ] 新しいパッケージ（dependency）の追加
- [ ] ナビゲーション構造の変更
- [ ] Supabaseテーブル構造・RPC の変更
- [ ] 既存ウィジェットの大幅なレイアウト変更
- [ ] ファイルの削除
- [ ] 3ファイル以上にまたがる変更

---

## 3. 技術スタック（固定）

変更する場合はユーザーの明示的な許可が必要。

| 区分 | 技術 | 変更可否 |
|------|------|----------|
| フレームワーク | Flutter | 変更不可 |
| 言語 | Dart | 変更不可 |
| 状態管理 | flutter_riverpod | 変更不可 |
| バックエンド | Supabase | 変更不可 |
| 位置情報 | geolocator | 変更不可 |
| デザイン | Material 3 | 変更不可 |

---

## 4. アーキテクチャルール

### 4.1 レイヤー構成（厳守）
```
screens/    → 画面（UIロジック）
widgets/    → 再利用可能UIコンポーネント
providers/  → Riverpod 状態管理
services/   → データ取得・外部通信
models/     → データモデル（イミュータブル）
```

- 各レイヤーの責務を混在させない
- `screens/` から直接 `services/` を呼ばない（`providers/` を経由する）
- `models/` にビジネスロジックを入れない
- `widgets/` は状態を持たない（Providerを直接watchしない）のが望ましい

### 4.2 命名規則
- ファイル名: `snake_case.dart`
- クラス名: `PascalCase`
- 変数・関数: `camelCase`
- Provider: `xxxProvider` （例: `locationProvider`）
- Service: `XxxService` （例: `StationService`）
- Notifier: `XxxNotifier` （例: `LocationNotifier`）
- 画面: `XxxScreen` （例: `HomeScreen`）
- ウィジェット: 用途を表す名前 （例: `StationCard`）

### 4.3 新規ファイル作成時のルール
- 既存ファイルへの追記で済む場合は新規ファイルを作らない
- 新規ファイルは必ず適切なディレクトリに配置する
- 1ファイル1クラスを基本とする（privateヘルパーは同一ファイル可）

---

## 5. コーディングルール

### 5.1 全般
- 不要なコメントを追加しない（コードで意図が明確な場合）
- `print()` / `debugPrint()` をプロダクションコードに残さない（デバッグ時のみ）
- `// TODO` を安易に増やさない。追加する場合は具体的な内容を記述する
- 未使用の import を残さない
- マジックナンバーは定数として定義する

### 5.2 Flutter / Dart 固有
- `const` コンストラクタを可能な限り使用する
- `StatelessWidget` で済む場合は `StatefulWidget` を使わない
- `BuildContext` をasync gap越しに使用しない（`mounted` チェック必須）
- モデルクラスはイミュータブル（`final` フィールド + `const` コンストラクタ）

### 5.3 エラーハンドリング
- `try-catch` で握りつぶさない（最低限ログ出力 or ユーザーへの通知）
- ユーザー向けエラーメッセージは日本語で表示する
- ローディング状態を必ず表示する（無限待機させない）

---

## 6. テストルール（必須）

詳細な観点・ピラミッド構成は `docs/testing-strategy.md` を参照すること。

### 6.1 テスト必須の原則
- **プロダクションコードを変更・追加したら、対応するテストも必ず書く/更新する**
- テストのないコード変更は不完全とみなす
- 既存テストを壊す変更を放置しない（`flutter test` が全件 pass する状態を維持）

### 6.2 変更種別ごとのテスト義務

| 変更内容 | 必須テスト |
|---------|-----------|
| Model 追加・変更 | fromJson / toJson / copyWith の Unit テスト |
| Service 追加・変更 | モックモード正常系・異常系の Unit テスト |
| Provider 追加・変更 | 初期状態・成功・失敗の状態遷移 Unit テスト |
| Widget 追加・変更 | 表示・タップの Widget テスト |
| Screen 追加・変更 | 正常系・空状態・エラー状態の Widget テスト |
| ロジック関数追加 | 入力→出力の Unit テスト |
| バグ修正 | バグを再現するテストを先に書き、修正後に pass することを確認 |

### 6.3 テスト品質基準
- テスト名は日本語で「○○の場合、○○になる」形式
- AAA パターン（Arrange → Act → Assert）で構成する
- テスト間で状態を共有しない（各テスト独立）
- `flutter test` を実行して全件 pass を確認してから作業完了とする

---

## 7. UIルール

- アプリの言語は **日本語** で統一する
- Material 3 のコンポーネント・カラーシステムに従う
- カラーシードは `#4299F0` を維持する（変更時は確認必須）
- ダークモード対応を壊さない（ハードコードされた色を使わない）
- アイコンは `Icons.xxx`（Material Icons）を使用する

---

## 8. モックモードのルール

- モックモード（`_useMock = true`）は開発・テスト用として維持する
- Supabase実装時もモックモードを削除せず、切り替え可能にする
- モックデータの追加・変更時は現実的なデータにする

---

## 9. プロダクト定義（逸脱禁止）

このアプリは **「位置情報ベースの匿名駅掲示板」** である。

### 含まれる機能（現在 + 計画）
- 現在地に基づく近隣駅の検索・表示
- 駅ごとの掲示板（スレッド一覧）
- スレッドの作成・閲覧
- スレッドへの返信投稿
- コンテンツの通報
- 設定画面（利用規約・プライバシーポリシー・バージョン）

### 明示的にスコープ外とするもの（追加禁止）
- ユーザー登録・ログイン機能（匿名性を維持）
- ダイレクトメッセージ・チャット機能
- SNS連携・シェア機能
- 広告表示
- 課金・サブスクリプション
- 地図表示・マップ機能
- プッシュ通知（将来検討だが現時点ではスコープ外）
- 画像・動画投稿（将来検討だが現時点ではスコープ外）

> **上記スコープ外の機能を追加する場合は、必ずユーザーに確認し、このセクションを更新すること。**

---

## 10. 変更履歴

このルールファイル自体の変更を記録する。

| 日付 | 変更内容 | 変更者 |
|------|----------|--------|
| 2026-02-14 | 初版作成 | Claude |
| 2026-02-16 | セクション6「テストルール（必須）」追加 | Claude |

---

## 11. ユーザー追加ルール

<!-- ユーザーが独自のルールをここに追記してください -->

