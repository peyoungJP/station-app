# 開発ガードレール

> Claude Code が毎セッション自動読み込みするルールファイル。

---

## コマンド

```bash
flutter test                          # 全テスト実行（作業完了前に必ず実行）
flutter test test/unit/               # Unit テストのみ
flutter test path/to/file_test.dart   # 特定ファイル
flutter analyze                       # 静的解析
flutter run                           # アプリ起動
```

---

## 禁止事項（NEVER）

- **指示されていない機能・ファイル追加・リファクタリングは一切行わない**
- データモデルのフィールド削除・型変更は必ず確認を取る
- 既存の Service メソッドシグネチャを無断で変更しない
- パッケージの削除・メジャーバージョン変更は必ず確認を取る
- IMPORTANT: `git push` を無断で実行しない
- IMPORTANT: コミットは必ずユーザーの明示的な指示を受けてから行う
- `main` / `master` ブランチへの直接コミット禁止（指示がある場合を除く）

---

## 必須確認事項（作業前に必ず確認）

- 新しい画面（Screen）の追加
- 新しいデータモデル（Model）の追加・変更
- 新しいパッケージ（dependency）の追加
- ナビゲーション構造の変更
- Supabaseテーブル構造・RPC の変更
- ファイルの削除
- 3ファイル以上にまたがる変更

---

## アーキテクチャ

```
screens/    → 画面（UIロジック）
widgets/    → 再利用可能UIコンポーネント
providers/  → Riverpod 状態管理
services/   → データ取得・外部通信
models/     → データモデル（イミュータブル）
```

- IMPORTANT: `screens/` から直接 `services/` を呼ばない（`providers/` 経由必須）
- `models/` にビジネスロジックを入れない
- `widgets/` は状態を持たない（Provider を直接 watch しない）のが望ましい

### 命名規則

- ファイル名: `snake_case.dart` / クラス名: `PascalCase` / 変数・関数: `camelCase`
- Provider: `xxxProvider` / Service: `XxxService` / Notifier: `XxxNotifier` / 画面: `XxxScreen`

---

## コーディングルール

- IMPORTANT: `BuildContext` を async gap 越しに使わない（`mounted` チェック必須）
- `debugPrint()` はデバッグ時のみ。コミット前に必ず削除する
- ユーザー向けエラーメッセージは日本語で表示する
- ローディング状態を必ず表示する（無限待機させない）

---

## テスト

詳細な観点・ピラミッド構成・実行コマンドは @docs/testing-strategy.md を参照。

IMPORTANT: **プロダクションコードを変更・追加したら、必ず対応するテストを書く/更新する。**

| 変更内容 | 必須テスト |
|---------|-----------|
| Model 追加・変更 | fromJson / toJson / copyWith |
| Service 追加・変更 | モックモード正常系・異常系 |
| Provider 追加・変更 | 初期状態・成功・失敗の状態遷移 |
| Widget / Screen 追加・変更 | 表示・タップ・エラー状態 |
| バグ修正 | バグ再現テストを先に書き、修正後に pass を確認 |

- テスト名は日本語「○○の場合、○○になる」形式
- `flutter test` 全件 pass を確認してから作業完了とする

---

## UI

- アプリの言語は**日本語**で統一
- カラーシードは `#4299F0` を維持（変更時は確認必須）
- ハードコードされた色を使わない（ダークモード対応を維持する）
- アイコンは `Icons.xxx`（Material Icons）を使用

---

## モックモード

- `_useMock = true` は開発・テスト用として常に維持する
- Supabase 実装後もモックモードを削除せず、切り替え可能にする

---

## プロダクト定義

このアプリは **「位置情報ベースの匿名駅掲示板」**。

### スコープ内

- 現在地に基づく近隣駅の検索・表示
- 駅ごとの掲示板（スレッド一覧）
- スレッドの作成・閲覧・返信投稿
- コンテンツの通報
- 設定画面（利用規約・プライバシーポリシー・バージョン）

### IMPORTANT: 追加禁止（スコープ外）

- ユーザー登録・ログイン（匿名性を維持）
- DM・チャット・SNS連携・シェア機能
- 広告・課金・サブスクリプション
- 地図表示・プッシュ通知・画像/動画投稿

> スコープ外機能を追加する場合は、必ずユーザーに確認しこのセクションを更新すること。

---

## ユーザー追加ルール

<!-- ユーザーが独自のルールをここに追記してください -->
